const menu=document.querySelector('.menu-btn');const links=document.querySelector('.nav-links');if(menu)menu.addEventListener('click',()=>links.classList.toggle('open'));
const bar=document.querySelector('.progress span');addEventListener('scroll',()=>{const h=document.documentElement.scrollHeight-innerHeight;bar.style.width=(h?scrollY/h*100:0)+'%'});
document.querySelectorAll('[data-answer]').forEach(btn=>btn.addEventListener('click',()=>{const group=btn.closest('.quiz');group.querySelectorAll('button').forEach(b=>{b.disabled=true;if(b.dataset.answer==='true')b.classList.add('correct')});if(btn.dataset.answer!=='true')btn.classList.add('wrong');const out=group.querySelector('.quiz-result');out.textContent=btn.dataset.answer==='true'?'أحسنت! إجابة صحيحة 🎉':'حاول مرة أخرى بعد مراجعة القاعدة.';}));
